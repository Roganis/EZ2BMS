// chart-core's messages in English: the source every translation is
// typed against. Filled area by area (lint, fixes, import and export notes).

export const coreEn = {};

export type CoreKey = keyof typeof coreEn;
